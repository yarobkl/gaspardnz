// Push notifications are intentionally hidden until a real provider is configured.
// The native permission flow in services/notifications.js stays disabled, so the
// public website must not promise a feature that cannot currently be delivered.
const NotificationPrompt = () => null;

export default NotificationPrompt;
